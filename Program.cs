namespace Demo;

class Program {

    /// <summary>
    /// The main entry point for this application,
    ///  its only job is to start the renderer
    /// </summary>
    static void Main() {

        PPR.Demos.GPU.GPUDemoRenderer demo = new();
    }
}