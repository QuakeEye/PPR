namespace PPR;


public static class Settings {

    // A couple of settings the template uses to initialise the project
    
    // The window size
    public static int Width = 1280;
    public static int Height = 720;

    // The title of the window
    public static string Title = "Per-Pixel Rendering";

    // Should non-error messages be displayed?
    public static bool Debug = true;
}